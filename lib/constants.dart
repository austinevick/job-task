import 'package:flutter/material.dart';

const primaryColor = Color(0xff0044C0);
const lightBlue = Color(0xffb2ceff);
final lightGrey = Colors.grey.withOpacity(0.3);

const styleThickBlue =
    TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: primaryColor);
const styleThickGrey = TextStyle(fontSize: 15, color: Colors.grey);
